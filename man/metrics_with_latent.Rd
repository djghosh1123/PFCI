% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pfci_metrics_latent.R
\name{metrics_with_latent}
\alias{metrics_with_latent}
\title{Metrics for latent simulation using oracle-FCI truth (skeleton only)}
\usage{
metrics_with_latent(sim, fit)
}
\arguments{
\item{sim}{Output from simulate_with_latent().}

\item{fit}{Output from pfci_fit() (must contain $amat and $time$total).}
}
\value{
A named list with SHD, F1_total, MCC, Time.
}
\description{
Designed for the 3-line workflow:
sim <- simulate_with_latent(...)
fit <- pfci_fit(sim$X, ...)
met <- metrics_with_latent(sim, fit)
}
\details{
Returns only: SHD, F1_total, MCC, Time.
}
\examples{
\donttest{
  sim <- simulate_with_latent(p_obs = 30, gamma = 0.05, n = 100, seed_graph = 1)
  fit <- pfci_fit(sim$X, alpha = 0.05)
  met <- metrics_with_latent(sim, fit)
  print(met)
}
}
\seealso{
\code{\link{simulate_with_latent}}, \code{\link{pfci_fit}}
}
