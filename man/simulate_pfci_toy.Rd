% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_pfci_toy.R
\name{simulate_pfci_toy}
\alias{simulate_pfci_toy}
\title{Simulate toy data for PFCI using topo-ordered DAG + rmvDAG}
\usage{
simulate_pfci_toy(
  p = NULL,
  sparsity = NULL,
  n = 100,
  edge_prob = 0.02,
  errDist = c("normal", "t4", "mixt3"),
  seed = 1L,
  p_obs = NULL,
  gamma = 0.1
)
}
\arguments{
\item{p}{Number of observed variables (preferred).}

\item{sparsity}{Number of nodes eligible for edges (<= p). Default p.}

\item{n}{Sample size.}

\item{edge_prob}{Edge probability among eligible nodes.}

\item{errDist}{Error distribution for pcalg::rmvDAG ("normal","t4","mixt3").}

\item{seed}{Random seed.}

\item{p_obs}{(legacy) alias for p.}

\item{gamma}{(legacy) ignored (kept only for backward compatibility).}
}
\value{
A list: X, truth (true_dag, adj_mat, skel, amat), meta
}
\description{
Workflow:
sim <- simulate_pfci_toy(...)
fit <- pfci_fun(sim$X, ...)
met <- pfci_metrics(sim, fit)
}
\details{
This simulator:
\itemize{
\item generates a \emph{topologically ordered} DAG (edges only i -> j for i < j)
\item simulates data via pcalg::rmvDAG with requested errDist
\item returns truth skeleton (undirected) and an "amat-style" truth from dag2cpdag
}

NOTE: The returned truth_amat is derived from the CPDAG of the generating DAG
(so it contains directed and o-o circle edges, but not latent-induced o-> / <->).

Backward-compat: accepts old args p_obs/gamma (ignored) so old vignettes won't fail.
}
\examples{
sim <- simulate_pfci_toy(p = 30, n = 100, edge_prob = 0.05, seed = 1)
str(sim$truth)
}
\seealso{
\code{\link{pfci_fit}}, \code{\link{pfci_metrics}}
}
